import { ReceiveResetEmailForm } from '../../index'
import type { Meta, StoryObj } from '@storybook/react'
const meta: Meta<typeof ReceiveResetEmailForm> = {
  title: 'organisms/ReceiveResetEmailForm',
  component: ReceiveResetEmailForm,
  tags: ['autodocs'],
}

export default meta

type Story = StoryObj<typeof ReceiveResetEmailForm>

export const Default: Story = {}
